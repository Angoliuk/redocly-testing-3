{
  "name": "acme-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "bundle": "redocly bundle openapi/openapi.yaml -o public/openapi.yaml",
    "lint:api": "redocly lint",
    "predev": "pnpm bundle",
    "dev": "next dev",
    "prebuild": "pnpm bundle",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@redocly/cli": "latest"
  }
}
