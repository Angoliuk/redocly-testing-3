{
  "name": "acme-api",
  "version": "1.0.0",
  "dependencies": {
    "@redocly/cli": "latest"
  },
  "private": true,
  "scripts": {
    "start": "redocly preview --product redoc",
    "test": "redocly lint",
    "build": "redocly build-docs openapi/openapi.yaml -o dist/index.html"
  }
}
